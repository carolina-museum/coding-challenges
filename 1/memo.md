# 思考ログ

1. Two Sum

## Step 1
リストの大きさの制約が　2 <= nums.length <= 10**4　で、二重のループなら十分にLTEしないと思ったので総当たりで実装しました。
同じ要素を2回足してはいけないので、for文のindexが同じ場合はcontinueしました😊

## Step 1 - 二分探索を使った解法
1回目の解法を改善できるかを考えてみました。
まずリストをO(nlogn)でソートして、次にリストの前から順番にその数が一つ目に返す数であると仮定して、targetから一つ目の数を引いた数が残りのリストに存在するかを探していきました。存在していればそれが二つ目に返す数です。探す時には二分探索を使ったので、探索の計算量はO(logn)です。
存在することさえ分かれば、その数のnumsでのインデックスを探すのはO(n)でできます。この部分の計算量はO(nlogn)です。

そして、一つ目だと仮定した数とそれをtargetから引いた二つ目の数のペアのインデックスのペアを返せば完了です👌

注意点として、一つ目の数と二つ目の数が同じだった場合、それぞれ別のインデックスを返す必要があります。そのために、一つ目の数のインデックス以外から二つ目の数のインデックスを探しました。この部分の計算量はO(n)です。

これで、合計の計算量はO(nlogn)になり、総当たりのO(n**2)よりも軽くなりました！


## Step 2 - 高速化・簡略化
二重ループの総当たりで全てのペアを確認する解法を改善しました。
外のループはそのままですが、中のループで最初の要素から最後の要素まで確認するのではなく、外のループのインデックスから最後の要素までを確認する様に変更しました👍
こうすることで、同じ要素を2回足すことを回避する一文を書かずに済みます。そして、同じペアを2回確認することがなくなるので、速くなりました。

## Step 2 - Pythonic
Pythonっぽい書き方のメモ。
for文を回す時に、enumerateを使えばインデックスと要素の両方を得ることができます🎉🎉🎉🎉
```
for i, num in enumerate(nums):
    print(i, num)
```
ドキュメント：https://docs.python.org/3/library/functions.html#enumerate
公式ドキュメントを見ることを覚えました😊

## Step 2 - ポインタを使った解法
ソートしたnumsの両端から組み合わせを探す解法です。ソートの計算量はO(nlogn)です。

まず、一番小さい要素（左）と一番大きい要素（右）を足してそれがtargetの値であればそれらのインデックスを返します。
もし左＋右がtargetよりも大きければ、右を一つ左にずらします。（右のインデックスを１減らします）。
ぎゃくに、左＋右がtargetよりも小さければ、左と一つ右にずらします。（左のインデックスを１増やします）。
これを繰り返していけば必ず正解のペアがみつかります。この部分の計算量はO(n)です。

最後にインデックスを返す時に、一つ目の数と二つ目の数が同じだった場合、それぞれ別のインデックスを返す必要があることに注意です！
この部分の計算量はO(n)です。

これで、合計の計算量はO(nlogn)になり、とても速くなりました🎉🎉🎉🎉

## Step 2 - dictionaryを使った解法
とっても天才！！！！ソートも必要ないし、リストを一巡するだけで解ける！！！速い！！！

dictionaryでの探索はO(1)であることをとっても活用した解法。
keyは数で、valueはインデックスにします。
リストの冒頭からその数が一つ目に返す数であると仮定して、targetから一つ目の数を引いた数がdictionaryに格納されているか探していきます。
ペアが見つかればいいので、ソートの必要はありません。

これで合計の計算量はO(n)です。


## Step 3 
最終的には一番シンプルで分かりやすい総当たりの解法を選びました。
総当たりでも十分に対応できる入力の制約で、二重ループでシンプルに表現したことで理解する労力が減り、
メンテナンスが楽になる、行数が減ってミスが減る、などのメリットがあり業務で役に立つのではないかなと想像しました。
他の解法は発想が豊かで、もっと制約が厳しい問題や複雑な問題を解くための力になりました。今まで出会わなかった解法に出会うことができてとても好奇心が満たされました！


## Step 4 - ポインタを使った解法
みなさんにレビューをいただいてコードをより良く書きかえました！
- 変数名。left --> left_num 左の何なのかが分かりやすくなりました。
- list.index()にリストをスライスして渡すのと同じ働きをしてくれる機能があることを知りました。https://docs.python.org/3/tutorial/datastructures.html
- インデントを減らして書く方法を取り入れてみました。
- while-else構文を頭の引き出しに入れました。
- 整え方のあたりを見るのが次のタスクです：https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.9kpbwslvv3yv

## Step 4 - 二分探索を使った解法
みなさんにレビューをいただいてコードをより良く書きかえました！
- 真偽値を返す関数なので名前をtarget_exists()にしました。アルゴリズムには二分探索を使っていますが、関数名にすると今回の場合はわかりにくくなってしまっていました。
- 再帰ではなくてwhileループを使った方法で二分探索を書きました。再帰には制限も多く、この方が好まれることが多いそうです。
- いただいた資料：https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.uvguf4c3q02d
- 条件を満たす組み合わせがない入力が入ってきた場合などエラー処理のことを考えることも忘れずに！利用する人のことを思いやります。
- bisect.bisect_leftというライブラリはソートされたリストに要素を挿入する時に使います。挿入すべきインデックスを教えてくれます。rightもあります。https://docs.python.org/3/library/bisect.html


## Tips
- 関数は必ず本来返すべき型の何かを返すようにすると良い。不意にNoneが返ってくるとびっくりしてしまうから。
業務で他のチームが関数を使った時に、型ヒントと食い違った値が返ってきてしまうと本来の挙動を予測するのが難しくなったり、ブラックボックスとして使うのが難しくなったりしてしまう。
- dictionaryはkey_to_valueになるように名付けると対応がわかりやすくて良い。
- return early: できるだけ早い段階でreturnしましょう。if文の他の条件には関係なく返す時は、ifの時点で返してしまえばそれに続くelif/elseブロックを読む必要がなくなります。他にも、最初のif文でその条件に当てはまる状況を排除したらすぐにreturnしてしまえば後の操作のことは考えずに済みます。また、後の操作をelseを使って書く必要がなくなるので、インデントが減ります。読みやすくなります。
- https://peps.python.org/pep-0020/ Zen!
- 計算量に厳密にこだわるのではなく、実際の入力条件やその他ユーザーの利益を考えて業務では計算量の削減に気を取られすぎないようにしましょう。
- ruff check file_name.py を活用しましょう✨